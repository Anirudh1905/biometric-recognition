apiVersion: v1
kind: Service
metadata:
  name: biometric-recognition
  labels:
    app: biometric-recognition
spec:
  type: NodePort
  selector:
    app: biometric-recognition
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080
      protocol: TCP
